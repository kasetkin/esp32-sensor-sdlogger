cmake_minimum_required(VERSION 3.22)

set(EXTRA_COMPONENT_DIRS "../components" "../modules")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
idf_build_set_property(MINIMAL_BUILD ON)
project(esp32-sensor-sdlogger-tests)
